# 2026-07-17

## smart-reserve 核心功能 Word 文档生成

**任务**：根据 smart-reserve 项目代码与运行截图（熔断器触发日志），输出 Word 文档，把项目核心功能以截图/代码/表格为证据写清楚。

**项目概览**：
- 业务场景：2026 跨年演唱会抢票（10万张票/400万用户/黄牛脚本）
- 技术栈：Go 1.26 + go-zero 1.10 + etcd + Redis + Kafka + MySQL + Docker Compose
- 四大治理能力：① etcd 注册中心 ② 双层限流 ③ Kafka 熔断 ④ Redis 降级；外加 ⑤ 10% 灰度路由
- 项目结构：apps/appointment/{api,rpc,rmq} + apps/user/rpc + deploy/mysql-init + stress-test.js (K6)

**生成的产物**：
- `C:\Users\czmud\projects\smart-reserve\output\smart-reserve-core-features.docx` (120.9 KB, 8 章/24 小节/14 表格/1 张内嵌截图)
- `C:\Users\czmud\projects\smart-reserve\output\circuit-breaker-evidence.png`（从 clipboard-images 复制，作为 @image#1 证据）

**截图解读（@image#1）**：
PowerShell 终端实拍日志，捕获到：
- 用户 2001 对场次 99 的重复预约（防重）
- ⚠️【故障模拟】模拟 Kafka 写入失败（连续 3 次）
- 🔴 [熔断器] CLOSED → OPEN！连续失败 3 次，触发熔断保护！← 关键证据

**关键技术细节**：
- 熔断阈值：Kafka 连续 3 次失败触发；冷却 30s 后进入 HALF_OPEN
- 限流：IP 5 QPS + 用户 10 QPS，基于 Redis PeriodLimit
- 降级：degradation:orders Redis List + 补偿 Worker
- 灰度：userId % 10 == 0 走 canary RPC（端口 9091）

**使用工具**：
- docx skill (cb_teams_marketplace) + npm `docx` 8.x 包
- 脚本位置：`C:\Users\czmud\.workbuddy\binaries\node\workspace\generate-docx.js`
- Node 运行时：`C:\Users\czmud\.workbuddy\binaries\node\versions\22.22.2\node.exe`
- 关键 API：Document, Packer, Paragraph, Table, TableCell, ImageRun, PageNumber, HeadingLevel

**注意事项**：
- 验证脚本需要 `defusedxml`，本次未装所以跳过 schema validate；通过 zip 结构性检查确认文档完整（28 个内部文件、所有必要 parts OK、图片正常嵌入）
- 脚本中 PROJECT_ROOT 硬编码为 `C:\Users\czmud\projects\smart-reserve`，不要使用 `__dirname` 因为脚本在 node workspace 目录而非项目目录
